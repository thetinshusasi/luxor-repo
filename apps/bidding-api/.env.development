# This should not be committed in Real repo. But commiting here for assignement purposes to 
# to make the project assessment easy  and there are no real secrets here

# Development environment configuration for bidding-api
NODE_ENV=development
PORT=3001
API_PREFIX=api

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=luxor_bidding
DB_USER=postgres
DB_PASSWORD=password

SYNCHRONIZE=false

# JWT configuration
JWT_SECRET=dev-jwt-secret-key
JWT_EXPIRES_IN=100000s
CACHE_TTL=3600

# CORS configuration
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=verbose

# Development specific settings
DEBUG=true
ENABLE_SWAGGER=true
ENABLE_CORS=true
